js执行函数时会默认完成以上的步骤，你可以把直接调用函数理解为一种语法糖
比如

    function say(word) {
       console.log(world);
    }
    say("Hello world");

    say.call(window, "Hello world");
以上可以把say("Hello world")看做是say.call(window,"Hello world")的语法糖。
这个结论非常关键



原型只存在于函数中，原型链才可以存在于所有引用类型中！
你可以打印一下obj1.prototype（原型），obj1.__proto__（原型链）。



  :checked        单选框或复选框被选中。


  Object setter https://segmentfault.com/q/1010000004670616?_ea=687630